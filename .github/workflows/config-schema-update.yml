name: Update docs content

on:
  push:
    paths:
      - 'extension/config.schema.json'

jobs:
  config-schema-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Create config.schema.md
        run: |
          npm install wetzel -g
          echo "CONFIGSCHEMADOCS=`wetzel extension/config.schema.json`" >> $GITHUB_ENV

      - name: Commit
        run: |
          git checkout origin docs
          git config user.name "GitHub Actions Bot"
          git config user.email "<actions@github.com>"
          echo $CONFIGSCHEMADOCS >> config.schema.md
          git add -f config.schema.md
          git commit -m "chore: Update config.schema.md"
          git push origin